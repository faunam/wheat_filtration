import numpy as np


def topic_vocab_counts():
    """Returns a matrix of word counts for each topic for the entire vocabulary.
    Arguments:

    Returns:
      (ndarray(n_topics, n_docs)): each entry is the total word count for that word in that topic
    """
    pass


def filter_corp(relevant_topics: list, key_words: list, super_key_words: list):
    # filters corpus by percentage of rel_topics, key_words, and super_key_words
    # returns subcorpus
    pass
